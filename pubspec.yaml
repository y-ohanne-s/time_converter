name: time_converter
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  device_preview: ^1.1.0
  flutter:
    sdk: flutter
  google_fonts: ^5.0.0
  http: ^1.0.0
  intl: ^0.18.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
